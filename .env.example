# Loopback-only backend. Do not change this to a public interface.
HOST=127.0.0.1
PORT=4310

# Runtime secrets do not belong in this file. Wallet tokens will be entered
# through the local application and retained only for the active session.
